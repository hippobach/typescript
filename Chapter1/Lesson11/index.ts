const sum = (a: number, b: number): number => {
  return a + b;
};

console.log(sum(3, 5));

const handleLogs = (log: string): void => {
  console.log('Xy ly log ', log);
};

handleLogs('Not Found - 404');
